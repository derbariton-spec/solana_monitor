# Solana Monitor 5.3 Icon Hotfix

Ersetzt `app.py` und ergänzt `assets/app-icon.png`.

Nach Upload: committen/pushen, Streamlit rebooten. Auf dem iPhone altes Home-Screen-Icon löschen und die App neu über Safari → Teilen → Zum Home-Bildschirm hinzufügen. iOS cached alte Icons oft.
